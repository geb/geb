include "core", 
        "test-utils", 
        "spock", 
        "junit3", 
        "junit4",
        "grails-support", 
        "grails-plugin"

if (hasProperty("withSamples")) {
	include "samples:google"
}

