include "core", 
        "test-utils", 
        "runners:spock", 
        "runners:junit3", 
        "grails:jar", 
        "grails:plugin"

if (hasProperty("withSamples")) {
	include "samples:google"
}

