include "module:core", 
        "module:test-utils", 
        "module:spock", 
        "module:junit3", 
        "module:junit4",
        "module:grails-support", 
        "module:grails-plugin",
        "doc:manual"

if (hasProperty("withSamples")) {
	include "samples:google"
}

